@*@model workforce_management.ViewModels.TrainingProgramDetail
@using Bangazon.Models;

<h2>@Html.DisplayFor(model => model.TrainingProgram.Name)</h2>
<a asp-action="Edit" asp-route-id="@Model.TrainingProgram.TrainingProgramId">Edit Program</a>

<div class="row">
    <div class="col-md-3">
        <h5>Description:</h5>
        <p>@Html.DisplayFor(model => model.TrainingProgram.Description)</p>
    </div>
    <div class="col-md-3">
        <h5>Date Created:</h5>
        <p>@Html.DisplayFor(model => model.TrainingProgram.DateCreated)</p>
    </div>
</div>

@foreach (Employee attendee in Model.Attendees)
{
    <div class="row">
        <div class="col-md-10">
            <div style="display:inline-block">
                <div><strong>@Html.DisplayFor(modelItem => attendee.FirstName) @Html.DisplayFor(modelItem => attendee.LastName)</strong></div>
                <div>@Html.DisplayFor(modelItem => attendee.Department.Name)</div>
            </div>

        </div>
    </div>
}*@

@model workforce_management.ViewModels.TrainingProgramDetail
@using Bangazon.Models;

<h1>
    @Html.DisplayFor(model => model.TrainingProgram.Name)
    <a asp-action="Edit" asp-route-id="@Model.TrainingProgram.TrainingProgramId"><span style="color:dodgerblue;font-size:0.5em" class="glyphicon glyphicon-pencil" aria-hidden="true"></span></a>
</h1>

<table class="table table-striped">
    <thead>
        <tr>
            <th>Training Program Name</th>
            <th>Description</th>
            <th>Date Added</th>
        </tr>
    </thead>
    <tbody>
        <tr>
            <td>@Html.DisplayFor(model => model.TrainingProgram.Name)</td>
            <td>@Html.DisplayFor(model => model.TrainingProgram.Description)</td>
            <td>@Html.DisplayFor(model => model.TrainingProgram.DateCreated)</td>
        </tr>
    </tbody>
</table>

<hr />

<h3>Attendees</h3>
@if (Model.Attendees.Count() > 0)
{
    <table class="table table-striped">
        <thead>
            <tr>
                <th>Employee Name</th>
                <th colspan="3">Computer Assigned</th>
                <th>Department</th>
                <th>Start Date</th>
            </tr>
        </thead>
        <tbody>
            @foreach (Employee attendee in Model.Attendees)
            {
                <tr>
                    <td><a asp-controller="Employee" asp-action="Edit" asp-route-id="@attendee.EmployeeId">@Html.DisplayFor(model => attendee.FirstName) @Html.DisplayFor(model => attendee.LastName)</a></td>
                    <td>@Html.DisplayFor(model => attendee.Computer.Make)</td>
                    <td>@Html.DisplayFor(model => attendee.Computer.Model)</td>
                    <td>SN: @Html.DisplayFor(model => attendee.Computer.SerialNumber)</td>
                    <td>@Html.DisplayFor(model => attendee.Department.Name)</td>
                    <td>@Html.DisplayFor(model => attendee.StartDate)</td>
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <p>No employees enrolled in the <b>@Html.DisplayFor(model => model.TrainingProgram.Name)</b> training program.</p>
}
